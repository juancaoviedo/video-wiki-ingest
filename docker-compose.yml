version: "3.9"

services:
  video-wiki-ingest:
    build: .
    ports:
      - "8000:8000"
    environment:
      - WHISPER_MODEL=${WHISPER_MODEL:-base}
    volumes:
      - ./downloads:/tmp/downloads
    restart: unless-stopped
